{
  "name": "libraw-forge-app-check",
  "private": true,
  "version": "0.0.0",
  "description": "T24 Part C: minimal Electron Forge app proving @janhapke/libraw loads and decodes from inside a packaged app.asar (native .node unpacked via packagerConfig.asar.unpack). Main process only, no window. Never published, never committed to node_modules/out/ (see .gitignore).",
  "main": "main.js",
  "scripts": {
    "package": "electron-forge package"
  },
  "devDependencies": {
    "@electron-forge/cli": "7.11.2",
    "electron": "42.11.2"
  },
  "dependencies": {
    "@janhapke/libraw": "file:../../janhapke-libraw-0.0.0.tgz"
  }
}
