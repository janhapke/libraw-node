{
  "$comment": "Hand-maintained (T14a). Keyed by exact C struct-type reference string under \"structs\" (the six top-level groups -- idata/sizes/other/lens/color/makernotes.common -- plus every nested struct type scripts/gen-metadata.js reaches from their fields, e.g. \"libraw_gps_info_t\", \"struct ph1_t\"), then by C field name verbatim. `type` is the JS-facing shape scripts/gen-metadata-cc.js renders (int|uint|uint64|float|double|string|bytes|int[]|float[]|matrix|struct|time|unsupported); `cType`/`cArrayDims`/`pointerDepth` are NOT written here -- scripts/gen-metadata.js fills those in from the header itself. `unset`, where given, is the value LibRaw leaves a field at when it was not filled in for this file (verified against libraw_types.h field comments and vendor/LibRaw/src/utils/init_close_utils.cpp's zero-initialization of imgdata; the C++ side turns that value into `undefined`/an omitted key) -- this mechanism is NOT used for `uint64` fields (see that type below), since their unset sentinel cannot survive a JSON round-trip as an exact 64-bit integer. `notes` documents why a field is `unsupported` (a raw pointer with no safe/useful way to read back, or an internal/huge table not part of the requested surface -- curve[65536], the two cblack[4104] tables, and white[8][8], per this generator's own header comment on scope) or any other non-obvious representation choice. `uint64` is LibRaw's 64-bit unsigned `UINT64` fields (currently only libraw_makernotes_lens_t's LensID/CamID/TeleconverterID/AdapterID/AttachmentID): scripts/gen-metadata-cc.js omits the key when the raw value equals UINT64_MAX (LibRaw's own unset sentinel for these, compared in C++ against std::numeric_limits<uint64_t>::max() -- never round-tripped through this JSON file), otherwise emits a JS Number when the value is <= Number.MAX_SAFE_INTEGER (2^53 - 1) and a JS BigInt (Napi::BigInt::New) when it is larger, so no precision is lost either way. Per-vendor makernotes (imgdata.makernotes.{canon,nikon,sony,fuji,olympus,panasonic,pentax,samsung,kodak,p1,hasselblad,ricoh}) are T14b, not covered here. Every field of every struct this generator reaches must have an entry here, and every entry here must name a real field, or `node scripts/gen-metadata.js --check` fails. Sources: vendor/LibRaw/libraw/libraw_types.h field comments, vendor/LibRaw/doc/API-datastruct.html, docs/explanation/libraw-api-surface.md, verified field by field for LibRaw 0.22.2.",

  "structs": {
    "libraw_iparams_t": {
      "guard": { "doc": "Internal struct-validity guard bytes; not meaningful to callers.", "type": "unsupported", "notes": "Internal LibRaw bookkeeping (not documented as a public field); no useful JS representation." },
      "make": { "doc": "Camera make, as parsed from the file (may need normalization -- see normalized_make).", "type": "string" },
      "model": { "doc": "Camera model, as parsed from the file (may need normalization -- see normalized_model).", "type": "string" },
      "software": { "doc": "Software/firmware string embedded in the file, if any.", "type": "string" },
      "normalized_make": { "doc": "Make normalized against LibRaw's camera database (canonical vendor name).", "type": "string" },
      "normalized_model": { "doc": "Model normalized against LibRaw's camera database (canonical model name).", "type": "string" },
      "maker_index": { "doc": "Index into LibRaw's internal maker table for normalized_make.", "type": "uint" },
      "raw_count": { "doc": "Number of RAW frames/images stored in this file (e.g. multi-shot/burst formats).", "type": "uint" },
      "dng_version": { "doc": "DNG version encoded as LibRaw packs it (0 if this is not a DNG file).", "type": "uint", "unset": 0 },
      "is_foveon": { "doc": "Non-zero if this is a Foveon (layered-sensor) file.", "type": "uint" },
      "colors": { "doc": "Number of colors in the RAW data (1, 3, or 4).", "type": "int" },
      "filters": { "doc": "CFA pattern bitmask LibRaw uses internally to decode the Bayer/X-Trans layout (0 or 9 for non-Bayer/X-Trans sensors).", "type": "uint" },
      "xtrans": { "doc": "6x6 X-Trans CFA pattern (color-plane indices 0..3), only meaningful when filters == 9.", "type": "matrix" },
      "xtrans_abs": { "doc": "6x6 X-Trans CFA pattern in absolute (unshifted) sensor coordinates.", "type": "matrix" },
      "cdesc": { "doc": "Color plane description string (e.g. \"RGBG\"), indexed by the values in filters/xtrans.", "type": "string" },
      "xmplen": { "doc": "Length in bytes of the embedded XMP packet (0 if none). See xmpdata for why the packet itself is not exposed.", "type": "uint", "unset": 0 },
      "xmpdata": { "doc": "Pointer to the embedded XMP packet's raw bytes.", "type": "unsupported", "notes": "Raw char* into LibRaw-owned memory; exposing it safely would need a Buffer copy keyed off xmplen, which is a reasonable future addition but out of scope for T14a's core-struct mirror." }
    },

    "libraw_image_sizes_t": {
      "raw_height": { "doc": "Full sensor height in raw pixels, before any cropping.", "type": "uint" },
      "raw_width": { "doc": "Full sensor width in raw pixels, before any cropping.", "type": "uint" },
      "height": { "doc": "Output image height in pixels, after LibRaw's standard crop.", "type": "uint" },
      "width": { "doc": "Output image width in pixels, after LibRaw's standard crop.", "type": "uint" },
      "top_margin": { "doc": "Rows skipped from the top of the raw sensor data to reach the cropped image.", "type": "uint" },
      "left_margin": { "doc": "Columns skipped from the left of the raw sensor data to reach the cropped image.", "type": "uint" },
      "iheight": { "doc": "Final output height after any half_size/other output-size-affecting params (equals height unless half_size etc. is used).", "type": "uint" },
      "iwidth": { "doc": "Final output width after any half_size/other output-size-affecting params (equals width unless half_size etc. is used).", "type": "uint" },
      "raw_pitch": { "doc": "Bytes per row of the raw (undecoded) sensor data.", "type": "uint" },
      "pixel_aspect": { "doc": "Pixel aspect ratio (width:height of one sensor pixel); 1.0 for square pixels.", "type": "double" },
      "flip": { "doc": "Orientation from the file: 0 none, 3 180deg, 5 90deg CCW, 6 90deg CW (other LibRaw-internal values are possible for less common sensors). See the `sizes.oriented` convenience for swapped width/height at flip 5/6.", "type": "int" },
      "mask": { "doc": "Up to 8 masked/black-frame border rectangles (each [top, left, bottom, right] in raw pixel coordinates); unused entries are all-zero.", "type": "matrix" },
      "raw_aspect": { "doc": "Raw sensor aspect-ratio hint LibRaw derives for a few multi-aspect sensors (0 if not applicable).", "type": "uint", "unset": 0 },
      "raw_inset_crops": { "doc": "Up to 2 inset-crop rectangles some makers embed (e.g. an in-camera crop suggestion), as libraw_raw_inset_crop_t.", "type": "struct" }
    },
    "libraw_raw_inset_crop_t": {
      "cleft": { "doc": "Inset crop left edge, in raw pixel coordinates.", "type": "uint", "unset": 65535, "notes": "LibRaw leaves an unfilled entry at {cleft: 0xffff, ctop: 0xffff, cwidth: 0, cheight: 0} -- verified empirically (synthetic DNG, no inset-crop tags)." },
      "ctop": { "doc": "Inset crop top edge, in raw pixel coordinates.", "type": "uint", "unset": 65535 },
      "cwidth": { "doc": "Inset crop width, in raw pixels.", "type": "uint" },
      "cheight": { "doc": "Inset crop height, in raw pixels.", "type": "uint" }
    },

    "libraw_imgother_t": {
      "iso_speed": { "doc": "ISO speed rating as parsed from the file's EXIF/maker data.", "type": "float" },
      "shutter": { "doc": "Shutter speed (exposure time) in seconds.", "type": "float" },
      "aperture": { "doc": "Aperture (f-number).", "type": "float" },
      "focal_len": { "doc": "Focal length in millimeters.", "type": "float" },
      "timestamp": { "doc": "Capture time as a Unix epoch value in seconds, exactly as LibRaw's time_t reports it (not converted to a JS Date -- construct one with `new Date(other.timestamp * 1000)` if needed).", "type": "time" },
      "shot_order": { "doc": "Shot sequence number within a burst/multi-shot file, where the format records one.", "type": "uint" },
      "gpsdata": { "doc": "Raw, undecoded GPS IFD tag words LibRaw captured while parsing.", "type": "unsupported", "notes": "Low-level parser intermediate; parsed_gps below is the decoded, useful form of the same data." },
      "parsed_gps": { "doc": "Decoded GPS data (latitude/longitude/altitude/timestamp and reference codes), as libraw_gps_info_t.", "type": "struct" },
      "desc": { "doc": "Free-text image description embedded in the file, if any.", "type": "string" },
      "artist": { "doc": "Artist/photographer name embedded in the file, if any.", "type": "string" },
      "analogbalance": { "doc": "DNG AnalogBalance tag (per-channel R,G,B,G2 multipliers applied before the color matrix), when present.", "type": "float[]" }
    },
    "libraw_gps_info_t": {
      "latitude": { "doc": "Latitude as [degrees, minutes, seconds].", "type": "float[]" },
      "longitude": { "doc": "Longitude as [degrees, minutes, seconds].", "type": "float[]" },
      "gpstimestamp": { "doc": "GPS timestamp (UTC) as [hours, minutes, seconds].", "type": "float[]" },
      "altitude": { "doc": "Altitude in meters.", "type": "float" },
      "altref": { "doc": "Altitude reference: '0' above sea level, '1' below sea level (ASCII digit character, not a number).", "type": "string", "unset": 0 },
      "latref": { "doc": "Latitude reference hemisphere: 'N' or 'S'.", "type": "string", "unset": 0 },
      "longref": { "doc": "Longitude reference hemisphere: 'E' or 'W'.", "type": "string", "unset": 0 },
      "gpsstatus": { "doc": "GPS receiver status code as recorded by the camera (maker-specific single character).", "type": "string", "unset": 0 },
      "gpsparsed": { "doc": "Non-zero once LibRaw has successfully parsed GPS data for this file (0 if the file has no GPS data).", "type": "uint" }
    },

    "libraw_lensinfo_t": {
      "MinFocal": { "doc": "Minimum focal length of the lens, in millimeters.", "type": "float", "unset": 0 },
      "MaxFocal": { "doc": "Maximum focal length of the lens, in millimeters.", "type": "float", "unset": 0 },
      "MaxAp4MinFocal": { "doc": "Maximum aperture (f-number) at MinFocal.", "type": "float", "unset": 0 },
      "MaxAp4MaxFocal": { "doc": "Maximum aperture (f-number) at MaxFocal.", "type": "float", "unset": 0 },
      "EXIF_MaxAp": { "doc": "Maximum aperture as reported by the EXIF MaxApertureValue tag.", "type": "float", "unset": 0 },
      "LensMake": { "doc": "Lens manufacturer, if recorded separately from the camera maker.", "type": "string" },
      "Lens": { "doc": "Lens model/name string, from whichever source (EXIF or maker-specific) LibRaw found first.", "type": "string" },
      "LensSerial": { "doc": "Lens serial number, if recorded.", "type": "string" },
      "InternalLensSerial": { "doc": "Lens's internal (maker-specific) serial number, if recorded.", "type": "string" },
      "FocalLengthIn35mmFormat": { "doc": "35mm-equivalent focal length in millimeters, from EXIF.", "type": "uint", "unset": 0 },
      "nikon": { "doc": "Nikon-specific lens fields, as libraw_nikonlens_t (populated regardless of camera make in a few cross-compatible cases).", "type": "struct" },
      "dng": { "doc": "DNG lens-info tags, as libraw_dnglens_t.", "type": "struct" },
      "makernotes": { "doc": "Vendor-agnostic lens-database fields LibRaw resolves from maker notes, as libraw_makernotes_lens_t.", "type": "struct" }
    },
    "libraw_nikonlens_t": {
      "EffectiveMaxAp": { "doc": "Effective maximum aperture (f-number) as decoded from Nikon lens data.", "type": "float", "unset": 0 },
      "LensIDNumber": { "doc": "Nikon lens ID byte (indexes Nikon's own lens database).", "type": "uint", "unset": 0 },
      "LensFStops": { "doc": "Number of f-stops in the lens's aperture range, Nikon encoding (1/12 EV units for some models -- see EXIF tools for the exact decode table).", "type": "uint", "unset": 0 },
      "MCUVersion": { "doc": "Lens MCU (electronic contacts) firmware version byte.", "type": "uint", "unset": 0 },
      "LensType": { "doc": "Nikon lens type flags byte (AF/AF-D/G/VR/etc. bit flags).", "type": "uint", "unset": 0 }
    },
    "libraw_dnglens_t": {
      "MinFocal": { "doc": "Minimum focal length of the lens, in millimeters, from DNG tags.", "type": "float", "unset": 0 },
      "MaxFocal": { "doc": "Maximum focal length of the lens, in millimeters, from DNG tags.", "type": "float", "unset": 0 },
      "MaxAp4MinFocal": { "doc": "Maximum aperture (f-number) at MinFocal, from DNG tags.", "type": "float", "unset": 0 },
      "MaxAp4MaxFocal": { "doc": "Maximum aperture (f-number) at MaxFocal, from DNG tags.", "type": "float", "unset": 0 }
    },
    "libraw_makernotes_lens_t": {
      "LensID": { "doc": "64-bit lens ID as decoded from maker notes (indexes LibRaw's lens database). Number when it fits Number.MAX_SAFE_INTEGER, otherwise a BigInt; the key is absent (not any sentinel value) when LibRaw has not filled it in.", "type": "uint64", "notes": "LibRaw's own unset value for this field is UINT64_MAX (all bits set, verified empirically on the synthetic DNG), not 0 -- handled specially by scripts/gen-metadata-cc.js's `uint64` case rather than this file's generic `unset` mechanism; see the top-level $comment." },
      "Lens": { "doc": "Lens name resolved from LibRaw's lens database using LensID.", "type": "string" },
      "LensFormat": { "doc": "Image-circle format the lens covers (LibRaw's internal lens-format enum value).", "type": "uint", "unset": 0 },
      "LensMount": { "doc": "Lens mount type ('male', the lens side; LibRaw's internal lens-mount enum value).", "type": "uint", "unset": 0 },
      "CamID": { "doc": "64-bit camera-body ID as decoded from maker notes (indexes LibRaw's camera database). Number when it fits Number.MAX_SAFE_INTEGER, otherwise a BigInt; the key is absent when LibRaw has not filled it in.", "type": "uint64", "notes": "Same unset note as LensID." },
      "CameraFormat": { "doc": "Sensor format of the camera body (LibRaw's internal camera-format enum value).", "type": "uint", "unset": 0 },
      "CameraMount": { "doc": "Camera mount type ('female', the body side; LibRaw's internal camera-mount enum value).", "type": "uint", "unset": 0 },
      "body": { "doc": "Camera body name associated with CamID.", "type": "string" },
      "FocalType": { "doc": "Lens focal-length type: -1 unknown, 0 unknown, 1 fixed focal length, 2 zoom.", "type": "int" },
      "LensFeatures_pre": { "doc": "Lens name prefix features (e.g. mount/format markers) LibRaw splits out of the full lens name.", "type": "string" },
      "LensFeatures_suf": { "doc": "Lens name suffix features (e.g. stabilization/AF markers) LibRaw splits out of the full lens name.", "type": "string" },
      "MinFocal": { "doc": "Minimum focal length of the lens, in millimeters, as resolved via the lens database.", "type": "float", "unset": 0 },
      "MaxFocal": { "doc": "Maximum focal length of the lens, in millimeters, as resolved via the lens database.", "type": "float", "unset": 0 },
      "MaxAp4MinFocal": { "doc": "Maximum aperture (f-number) at MinFocal, as resolved via the lens database.", "type": "float", "unset": 0 },
      "MaxAp4MaxFocal": { "doc": "Maximum aperture (f-number) at MaxFocal, as resolved via the lens database.", "type": "float", "unset": 0 },
      "MinAp4MinFocal": { "doc": "Minimum aperture (f-number) at MinFocal, as resolved via the lens database.", "type": "float", "unset": 0 },
      "MinAp4MaxFocal": { "doc": "Minimum aperture (f-number) at MaxFocal, as resolved via the lens database.", "type": "float", "unset": 0 },
      "MaxAp": { "doc": "Maximum aperture (f-number) across the lens's zoom range.", "type": "float", "unset": 0 },
      "MinAp": { "doc": "Minimum aperture (f-number) across the lens's zoom range.", "type": "float", "unset": 0 },
      "CurFocal": { "doc": "Focal length at time of shooting, in millimeters, as resolved via the lens database.", "type": "float", "unset": 0 },
      "CurAp": { "doc": "Aperture (f-number) at time of shooting, as resolved via the lens database.", "type": "float", "unset": 0 },
      "MaxAp4CurFocal": { "doc": "Maximum aperture (f-number) available at CurFocal.", "type": "float", "unset": 0 },
      "MinAp4CurFocal": { "doc": "Minimum aperture (f-number) available at CurFocal.", "type": "float", "unset": 0 },
      "MinFocusDistance": { "doc": "Minimum focus distance of the lens, in meters.", "type": "float", "unset": 0 },
      "FocusRangeIndex": { "doc": "Lens focus-range index (maker-specific classification of the lens's focus range).", "type": "float", "unset": 0 },
      "LensFStops": { "doc": "Number of f-stops in the lens's aperture range, as resolved via the lens database.", "type": "float", "unset": 0 },
      "TeleconverterID": { "doc": "64-bit teleconverter ID, if a teleconverter was attached and recognized. Number when it fits Number.MAX_SAFE_INTEGER, otherwise a BigInt; the key is absent when unset.", "type": "uint64", "notes": "Same unset note as LensID." },
      "Teleconverter": { "doc": "Teleconverter name resolved from TeleconverterID.", "type": "string" },
      "AdapterID": { "doc": "64-bit lens-mount adapter ID, if an adapter was recognized. Number when it fits Number.MAX_SAFE_INTEGER, otherwise a BigInt; the key is absent when unset.", "type": "uint64", "notes": "Same unset note as LensID." },
      "Adapter": { "doc": "Adapter name resolved from AdapterID.", "type": "string" },
      "AttachmentID": { "doc": "64-bit attachment (e.g. close-up filter) ID, if recognized. Number when it fits Number.MAX_SAFE_INTEGER, otherwise a BigInt; the key is absent when unset.", "type": "uint64", "notes": "Same unset note as LensID." },
      "Attachment": { "doc": "Attachment name resolved from AttachmentID.", "type": "string" },
      "FocalUnits": { "doc": "Divisor to convert the lens database's internal focal-length units to millimeters (1 in the common case).", "type": "uint", "unset": 0 },
      "FocalLengthIn35mmFormat": { "doc": "35mm-equivalent focal length in millimeters, as resolved via the lens database.", "type": "float", "unset": 0 }
    },

    "libraw_colordata_t": {
      "curve": { "doc": "Internal 65536-entry linearization/tone lookup table LibRaw builds during processing.", "type": "unsupported", "notes": "Only meaningful mid-decode (after unpack, before/around dcraw_process) and not part of the metadata this generator mirrors -- huge (65536 entries) and internal." },
      "cblack": { "doc": "Per-channel/per-pattern black level correction table (LIBRAW_CBLACK_SIZE entries; encodes both simple per-color-channel and complex per-line/per-pixel-pattern black levels depending on the camera).", "type": "unsupported", "notes": "4104-entry internal table (LIBRAW_CBLACK_SIZE); its first few entries are the common per-channel case, the rest a maker-specific pattern encoding not worth exposing raw. color.black/color.maximum below cover the common case." },
      "black": { "doc": "Overall black level (baseline dark-current offset) applied during processing.", "type": "uint" },
      "data_maximum": { "doc": "Maximum raw pixel value actually found in this file's data.", "type": "uint" },
      "maximum": { "doc": "Maximum sensor value (saturation point) LibRaw uses for highlight handling.", "type": "uint" },
      "linear_max": { "doc": "Per-channel linear maximum values (R,G,B,G2), used for highlight reconstruction on some formats.", "type": "int[]" },
      "fmaximum": { "doc": "Floating-point maximum sensor value, for floating-point RAW formats.", "type": "float" },
      "fnorm": { "doc": "Floating-point normalization factor, for floating-point RAW formats.", "type": "float" },
      "white": { "doc": "Per-corner (8x8 grid) white-level sample table some cameras embed for vignette/shading correction.", "type": "unsupported", "notes": "Internal shading-correction sample grid, not part of the requested color metadata surface." },
      "cam_mul": { "doc": "As-shot white balance multipliers (R, G1, B, G2), from the camera/file.", "type": "float[]" },
      "pre_mul": { "doc": "Pre-multipliers LibRaw computed for the sensor/file (used to build the final white balance when no as-shot data is available).", "type": "float[]" },
      "cmatrix": { "doc": "3x4 color matrix (camera RGB -> a working color space) LibRaw computed for this file.", "type": "matrix" },
      "ccm": { "doc": "3x4 color correction matrix embedded in the file (e.g. DNG ColorMatrix), before LibRaw's own adjustments.", "type": "matrix" },
      "rgb_cam": { "doc": "3x4 matrix converting camera-native RGB to output RGB, as used by dcraw_process.", "type": "matrix" },
      "cam_xyz": { "doc": "4x3 matrix converting camera-native RGB to CIE XYZ.", "type": "matrix" },
      "phase_one_data": { "doc": "Phase One-specific decoding parameters, as struct ph1_t (format, black-level geometry).", "type": "struct" },
      "flash_used": { "doc": "Flash compensation/power value as parsed from the file, when available.", "type": "float" },
      "canon_ev": { "doc": "Canon-specific exposure value adjustment parsed from maker notes.", "type": "float" },
      "model2": { "doc": "Secondary/internal model string some files embed (distinct from idata.model).", "type": "string" },
      "UniqueCameraModel": { "doc": "DNG UniqueCameraModel tag value.", "type": "string" },
      "LocalizedCameraModel": { "doc": "DNG LocalizedCameraModel tag value.", "type": "string" },
      "ImageUniqueID": { "doc": "DNG/EXIF ImageUniqueID tag value.", "type": "string" },
      "RawDataUniqueID": { "doc": "DNG RawDataUniqueID tag value (hex-encoded identifier).", "type": "string" },
      "OriginalRawFileName": { "doc": "DNG OriginalRawFileName tag value, for a DNG converted from another raw format.", "type": "string" },
      "profile": { "doc": "Embedded ICC color profile bytes, when the file carries one.", "type": "bytes", "notes": "void* pointer into LibRaw-owned memory, paired with profile_length below; scripts/gen-metadata-cc.js special-cases this field to copy profile_length bytes into a fresh Buffer when the pointer is non-null, and to omit the key entirely (never an empty Buffer) when it is null -- the one documented exception to \"pointer fields are unsupported\" (docs/plan/tasks.md's T14a Do list)." },
      "profile_length": { "doc": "Length in bytes of the embedded ICC profile (0 if color.profile is absent).", "type": "uint", "unset": 0 },
      "black_stat": { "doc": "Internal black-level statistics accumulator (sum/count pairs) used while computing color.black.", "type": "unsupported", "notes": "Internal accumulator, not a documented public metadata field." },
      "dng_color": { "doc": "Up to 2 DNG color calibration sets (e.g. for two calibration illuminants), as libraw_dng_color_t.", "type": "struct" },
      "dng_levels": { "doc": "DNG black/white level and crop metadata, as libraw_dng_levels_t.", "type": "struct" },
      "WB_Coeffs": { "doc": "Per-illuminant white balance coefficient presets (256 possible illuminant/CCT slots, [R, G1, B, G2] each) LibRaw parsed from the file's maker notes.", "type": "matrix", "notes": "Compacted: scripts/gen-metadata-cc.js emits only the illuminant slots that are actually set (any non-zero coefficient) as `[{ illuminant, coeffs: [r, g, b, g2] }]`, per docs/plan/tasks.md's T14a Do list, instead of the full fixed-size 256-entry table (almost entirely zero for any given file)." },
      "WBCT_Coeffs": { "doc": "Per-color-temperature white balance coefficient presets (64 possible slots, [CCT, R, G1, B, G2] each) some makers embed.", "type": "matrix", "notes": "Compacted the same way as WB_Coeffs, keyed on CCT > 0 instead of illuminant index: `[{ colorTemperature, coeffs: [r, g, b, g2] }]`." },
      "as_shot_wb_applied": { "doc": "Non-zero if the as-shot white balance was already applied to the raw data by the camera/converter (rare; e.g. some compressed raw formats).", "type": "uint" },
      "P1_color": { "doc": "Up to 2 Phase One color calibration sets, as libraw_P1_color_t.", "type": "struct" },
      "raw_bps": { "doc": "Bits per pixel/sample of the raw data (Phase One: raw format code instead -- see the header's field comment for the code table).", "type": "uint" },
      "ExifColorSpace": { "doc": "EXIF ColorSpace tag value (e.g. 1 = sRGB, 0xffff = uncalibrated).", "type": "int" }
    },
    "struct ph1_t": {
      "format": { "doc": "Phase One raw format code.", "type": "int" },
      "key_off": { "doc": "Phase One decryption key offset within the file.", "type": "int" },
      "tag_21a": { "doc": "Raw value of Phase One maker-note tag 0x21a (undocumented by Phase One; used internally by the decoder).", "type": "int" },
      "t_black": { "doc": "Phase One black-level tag value.", "type": "int" },
      "split_col": { "doc": "Column at which the sensor's dual-readout halves split, for split-sensor Phase One backs.", "type": "int" },
      "black_col": { "doc": "Column index of the black-reference columns.", "type": "int" },
      "split_row": { "doc": "Row at which the sensor's dual-readout halves split, for split-sensor Phase One backs.", "type": "int" },
      "black_row": { "doc": "Row index of the black-reference rows.", "type": "int" },
      "tag_210": { "doc": "Raw value of Phase One maker-note tag 0x210 (undocumented by Phase One; used internally by the decoder).", "type": "float" }
    },
    "libraw_dng_color_t": {
      "parsedfields": { "doc": "Bitmask of which DNG color-calibration fields this set actually parsed from the file.", "type": "uint" },
      "illuminant": { "doc": "DNG CalibrationIlluminant tag value (standard illuminant code) this calibration set applies to.", "type": "uint" },
      "calibration": { "doc": "4x4 DNG CameraCalibration matrix.", "type": "matrix" },
      "colormatrix": { "doc": "4x3 DNG ColorMatrix (camera RGB -> XYZ under the calibration illuminant).", "type": "matrix" },
      "forwardmatrix": { "doc": "3x4 DNG ForwardMatrix (XYZ -> camera RGB, the inverse-direction calibration some DNGs embed).", "type": "matrix" }
    },
    "libraw_dng_rawopcode_t": {
      "len": { "doc": "Length in bytes of this DNG opcode-list entry's raw data.", "type": "uint" },
      "data": { "doc": "Pointer to this DNG opcode-list entry's raw (undecoded) opcode data.", "type": "unsupported", "notes": "Raw void* into LibRaw-owned memory; DNG opcode lists are an advanced, format-specific feature out of scope for T14a's core mirror." }
    },
    "libraw_dng_levels_t": {
      "parsedfields": { "doc": "Bitmask of which DNG level/crop fields this struct actually parsed from the file.", "type": "uint" },
      "dng_cblack": { "doc": "DNG per-pattern black level table (LIBRAW_CBLACK_SIZE entries), the DNG-tag counterpart of color.cblack.", "type": "unsupported", "notes": "Same rationale as color.cblack above: a 4104-entry internal table." },
      "dng_black": { "doc": "DNG BlackLevel tag value (single black level), when the file uses the simple (non-per-pattern) form.", "type": "uint" },
      "dng_fcblack": { "doc": "Floating-point DNG per-pattern black level table (LIBRAW_CBLACK_SIZE entries), for floating-point DNGs.", "type": "unsupported", "notes": "Same rationale as dng_cblack above." },
      "dng_fblack": { "doc": "Floating-point DNG BlackLevel tag value, for floating-point DNGs.", "type": "float" },
      "dng_whitelevel": { "doc": "DNG WhiteLevel tag values, one per color plane.", "type": "int[]" },
      "default_crop": { "doc": "DNG DefaultCropOrigin + DefaultCropSize as [originX, originY, width, height].", "type": "int[]" },
      "user_crop": { "doc": "DNG user-crop rectangle, relative to default_crop, as [top, left, bottom, right] fractions.", "type": "float[]" },
      "preview_colorspace": { "doc": "DNG PreviewColorSpace tag value.", "type": "uint" },
      "analogbalance": { "doc": "DNG AnalogBalance tag (per-channel R,G,B,G2 multipliers), as parsed directly into this DNG-specific struct.", "type": "float[]" },
      "asshotneutral": { "doc": "DNG AsShotNeutral tag (per-channel neutral white balance values).", "type": "float[]" },
      "baseline_exposure": { "doc": "DNG BaselineExposure tag value (EV adjustment recommended by the DNG author).", "type": "float" },
      "LinearResponseLimit": { "doc": "DNG LinearResponseLimit tag value (fraction of full scale where the sensor's response stops being linear).", "type": "float" },
      "rawopcodes": { "doc": "Up to 3 DNG opcode-list entries (OpcodeList1/2/3), as libraw_dng_rawopcode_t.", "type": "struct" }
    },
    "libraw_P1_color_t": {
      "romm_cam": { "doc": "3x3 ROMM (ProPhoto RGB reference space) to camera-RGB matrix, flattened row-major to 9 values, for Phase One files.", "type": "float[]" }
    },

    "libraw_metadata_common_t": {
      "FlashEC": { "doc": "Flash exposure compensation, in EV.", "type": "float" },
      "FlashGN": { "doc": "Flash guide number.", "type": "float" },
      "CameraTemperature": { "doc": "Camera body internal temperature in degrees Celsius, when recorded.", "type": "float" },
      "SensorTemperature": { "doc": "Sensor temperature in degrees Celsius, when recorded.", "type": "float" },
      "SensorTemperature2": { "doc": "Secondary sensor temperature reading in degrees Celsius, when the camera records more than one.", "type": "float" },
      "LensTemperature": { "doc": "Lens temperature in degrees Celsius, when recorded.", "type": "float" },
      "AmbientTemperature": { "doc": "Ambient (environment) temperature in degrees Celsius, when recorded.", "type": "float" },
      "BatteryTemperature": { "doc": "Battery temperature in degrees Celsius, when recorded.", "type": "float" },
      "exifAmbientTemperature": { "doc": "Ambient temperature in degrees Celsius, from the EXIF/MakerNote ambient-temperature tag specifically.", "type": "float" },
      "exifHumidity": { "doc": "Relative humidity percentage, from EXIF, when recorded.", "type": "float" },
      "exifPressure": { "doc": "Atmospheric pressure in hPa, from EXIF, when recorded.", "type": "float" },
      "exifWaterDepth": { "doc": "Water depth in meters, from EXIF, for underwater housings that record it.", "type": "float" },
      "exifAcceleration": { "doc": "Acceleration magnitude, from EXIF, when recorded.", "type": "float" },
      "exifCameraElevationAngle": { "doc": "Camera elevation angle in degrees, from EXIF, when recorded.", "type": "float" },
      "real_ISO": { "doc": "Measured (actual) ISO sensitivity, when the camera records one distinct from the nominal ISO in other.iso_speed.", "type": "float" },
      "exifExposureIndex": { "doc": "EXIF ExposureIndex tag value.", "type": "float" },
      "ColorSpace": { "doc": "EXIF/MakerNote color space code (maker-specific encoding; compare against the relevant vendor's documented values).", "type": "uint" },
      "firmware": { "doc": "Camera firmware version string, when recorded outside idata.software.", "type": "string" },
      "ExposureCalibrationShift": { "doc": "Exposure calibration shift, in EV, some cameras record as a fine metering correction.", "type": "float" },
      "afdata": { "doc": "Autofocus-related maker-note data blocks actually present in this file (see afcount), as libraw_afinfo_item_t.", "type": "struct", "notes": "Fixed-size LIBRAW_AFDATA_MAXCOUNT (4) slots in the C struct; scripts/gen-metadata-cc.js emits only the first `afcount` entries (the rest are unused/zero slots)." },
      "afcount": { "doc": "Number of afdata entries actually populated (0..4).", "type": "int" }
    },
    "libraw_afinfo_item_t": {
      "AFInfoData_tag": { "doc": "Maker-note tag ID this autofocus data block was read from.", "type": "uint" },
      "AFInfoData_order": { "doc": "Byte order (TIFF-style, e.g. 0x4949/0x4d4d) this autofocus data block was encoded with.", "type": "int" },
      "AFInfoData_version": { "doc": "Version number/tag of this autofocus data block's internal format.", "type": "uint" },
      "AFInfoData_length": { "doc": "Length in bytes of this autofocus data block's raw payload.", "type": "uint" },
      "AFInfoData": { "doc": "Pointer to this autofocus data block's raw (undecoded) payload bytes.", "type": "unsupported", "notes": "Raw uchar* into LibRaw-owned memory; decoding autofocus point layouts is maker-specific and out of scope for T14a's core mirror." }
    }
  }
}
